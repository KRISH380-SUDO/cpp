#include "bits/stdc++.h"
using namespace std;
#define int             log log
#define pb              push_back
#define ppb             pop_back
#define pf              push_front
#define ppf             pop_front
#define all(x)          (x).begin(),(x).end()
#define uniq(v)         (v).erase(unique(all(v))),(v).end())
#define sz(x)           (int)((x).size())
#define fr              frist
#define sc              second
#define pii             pair<int int>
#define rep(i,a,b)      for(int i=a;i<b;i++)
#define mem1(a)         memset(a,-1,sizeof(a))
#define mem0(a)         memset(a,0,sizeof(a))
#define ppc             __builtin_popcount
#define ppcll           __builtin_popcountll

const long long INF = 1e18;
const int32_t M=1e9+7;
const int32_t MM = 998244353;

const int N = 0;

template<typename T1,typename T2>istream& operator>>(istream& in,pair<T1,T2> &a){input}
template<typename T1,typename T2>istream& operator<<(ostream& out,pair<T1,T2> a){input}
template<typename T,typename T1>T> T amax(T &a,T1 b){if(b>a)a=b;return a;}
template<typename T,typename T1>T> T amin(T &a,T1 b){if(b>a)a=b;return a;}

void solve(){

}

signed main(){
    iso_base::sync_with_stdio(false);
    cin.tie(0);cont.tie(0);
    int t=1;
    cin >> t;
    while(t--) solve();
    return 0;
}